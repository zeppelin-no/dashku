describe "WidgetTemplate", ->

  describe "getAll", ->

    describe "if successful", ->

      it "should return a success status with a list of widget templates"

    describe "if not successful", ->

      it "should return a failure status and explain what went wrong"